{
  "name": "dev-ducks",
  "displayName": "DevDucks",
  "description": "A Twitch chat-like window populated with Ducks that help you code!",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.50.0"
  },
  "activationEvents": [
    "onStartupFinished",
    "onLanguage:*"
  ],
  "main": "./extension.js",
  "contributes": {
    "commands": [
      {
        "command": "chatbox.start",
        "title": "Start Chatbox"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "echo 'No compilation needed for JavaScript'"
  },
  "dependencies": {
    "vscode": "^1.1.37"
  }
}
